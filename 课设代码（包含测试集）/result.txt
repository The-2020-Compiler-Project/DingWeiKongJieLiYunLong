DSEG	SEGMENT
LIN	DW	?
f	DW	?
g	DW	?
d	DW	?
e	DW	?
s	DW	?
p	DW	?
m	DW	7
a	DW	5
b	DW	6
t0	DW	?
t2	DW	?
c	DW	?
DSEG	ENDS
CSEG	SEGMENT
	ASSUME	CS:CSEG,DS:DSEG
fun3	PROC
	MOV	AX,g
	MOV	f,AX
	MOV	AX,f
	MOV	LIN,AX
	RET
fun3	ENDP
fun2	PROC
	MOV	AX,d
	MOV	f,AX
	MOV	AX,e
	MOV	g,AX
	CALL	fun3
	MOV	AX,LIN
	MOV	d,AX
	MOV	AX,d
	MOV	LIN,AX
	RET
fun2	ENDP
fun	PROC
	MOV	AX,s
	MOV	d,AX
	MOV	AX,p
	MOV	e,AX
	CALL	fun2
	MOV	AX,LIN
	MOV	s,AX
	MOV	AX,s
	MOV	LIN,AX
	RET
fun	ENDP
START:	MOV	AX,DSEG
	MOV	DS,AX
	MOV	AX,6
	MOV	BX,5
	CMP	AX,BX
	JA	JMP0
	MOV	t0,0
	JMP	JMP00
JMP0:	MOV	t0,1
JMP00:
	MOV	AX,t0
	CMP	AX,0
	JE	ELSE0
	MOV	AX,a
	ADD	AX,1
	MOV	a,AX
WHILE0:
	MOV	AX,b
	MOV	BX,a
	CMP	AX,BX
	JB	JMP1
	MOV	t2,0
	JMP	JMP11
JMP1:	MOV	t2,1
JMP11:
	MOV	AX,t2
	CMP	AX,0
	JE	WHEND0
	MOV	AX,a
	SUB	AX,1
	MOV	a,AX
	JMP	WHILE0
WHEND0:
	MOV	AX,a
	MOV	s,AX
	MOV	AX,b
	MOV	p,AX
	CALL	fun
	MOV	AX,LIN
	MOV	c,AX
	JMP	IFEND0
ELSE0:
	MOV	AX,b
	MOV	c,AX
IFEND0:
	INT	3
CSEG	ENDS
	END	START
